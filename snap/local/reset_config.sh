#!/bin/sh -e

# All parameters are unset
# leaving the default values up to the launchfile.

snapctl set port!
snapctl set address!
snapctl set tls!
snapctl set certfile!
snapctl set keyfile!
snapctl set topic_whitelist!
snapctl set param_whitelist!
snapctl set service_whitelist!
snapctl set client_topic_whitelist!
snapctl set min_qos_depth!
snapctl set max_qos_depth!
snapctl set num_threads!
snapctl set send_buffer_limit!
snapctl set use_sim_time!
snapctl set use-compression!
snapctl set capabilities!
snapctl set include_hidden!
snapctl set asset_uri_allowlist!
